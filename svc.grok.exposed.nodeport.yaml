apiVersion: v1
kind: Service
metadata:
  name: grok-service-exposed-nodeport
  namespace: gok
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: grok
